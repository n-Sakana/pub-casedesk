・手動実行（run export）のときに、自動実行用の設定がフォームに入力された状態で表示される
・共有メールの新着を検知できない
・各種設定が「all」のときはフォルダを分けて出力でいいが、メールアドレスやフォルダやキーワードを指定しているときは、設定した出力パス直下に出力してほしい（無駄にネストが深くなるだけなので）
・自動実行用の設定は５つまで保存できるように
・リファクタリングでコードをきれいに、高速化・フリーズ対策も忘れず
・キーワードはandやor組み合わせられるようにしたい。UIを複雑にしたくないので、一般的な記法で現行のテキストボックスに入力する形。例：A AND (B OR NOT C)
・.doc形式の添付ファイルの保存が失敗する
・エクセル単体で使える常駐型アプリケーションに。fe-be分離でフリーズなし
・日付フィルタは○日前ではなく、期間で設定する形式に（○日前、だと開始日を固定できない）
・ログをアプリ内部に保存し、基本情報をEXCELでみれるように
・自動実行で作成したTSVファイルに、手動実行で作成したメールの記録（出力先もフィルタも全く別）が混ざっているように見える
・tsvファイルは、保存のたびに重複して追記されてしまうのでは？（少なくとも、フォルダを削除してやり直す場合など）